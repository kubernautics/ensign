#!/bin/ansible-playbook 
# Build a K3s "cluster" on GCP Instance with full DNS resolution

# - [Build GCP DNS Zone] ---------------------
- import_playbook: gcp-dns

# - [Build GCP instance] ---------------------
- import_playbook: gcp-ensign

# - [Build GCP K3s on the instance] ----------
- import_playbook: ensign-k3s

# - [Build GCP OpenWRT Gateway the instance] -
#- import_playbook: ensign-gateway
