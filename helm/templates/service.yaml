apiVersion: v1
kind: Service
metadata:
  name: {{ include "ensign.fullname" . }}
  labels:
    {{- include "ensign.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "ensign.selectorLabels" . | nindent 4 }}
